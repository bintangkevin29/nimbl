import {StyleSheet} from 'react-native';

export const extraStackStyle = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: 42,
    flexWrap:'wrap'
  },
  extraContainer: {
    flex: 1,
  },
});
